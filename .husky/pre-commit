echo 'Checking for linting errors an run tests before committing'
export NVM_DIR="$HOME/.nvm"
. "$NVM_DIR/nvm.sh"
nvm use
pnpm lint || (echo 'Linting errors or tests failed. Please fix them before committing.' && exit 1)
echo 'Done !'